data:
  split_path: /fast_scratch/ipress/egamma/training/combine_combine_egamma/train0.7_val0.15_test0.15000000000000005.npz
  dataset:
    h5file: '/fast_scratch/ipress/egamma/training/combine_combine_egamma/combine_combine_egamma.hy'
    _target_: watchmal.dataset.cnn.cnn_dataset.CNNDataset
    pmt_positions_file: /home/ipress/t2k_ml_training/data/sk_wcsim_imagefile.npy
model:
  _recursive_: false
  _target_: watchmal.model.classifier.Classifier
  num_classes: 2
  feature_extractor:
    _target_: watchmal.model.resnet.resnet18
    num_input_channels: 2
    num_output_channels: 2
  classification_network:
    _target_: watchmal.model.classifier.PassThrough
engine:
  _target_: watchmal.engine.engine_classifier.ClassifierEngine
  label_set: 
  - 0
  - 1
tasks:
  train:
    epochs: 0
    report_interval: 100
    val_interval: 100
    num_val_batches: 32
    checkpointing: false
    early_stopping_patience: 3
    restore_best_state: False
    data_loaders:
      train:
        split_key: train_idxs
        batch_size: 32
        num_workers: 4
        transforms:
        - horizontal_flip
        - vertical_flip
        - front_back_reflection
        sampler:
          _target_: torch.utils.data.sampler.SubsetRandomSampler
      validation:
        split_key: val_idxs
        batch_size: 128
        num_workers: 4
        sampler:
          _target_: torch.utils.data.sampler.SubsetRandomSampler
    optimizers:
      _target_: torch.optim.Adam
      lr: 0.0001
      weight_decay: 0
    scheduler:
      _target_: torch.optim.lr_scheduler.ExponentialLR
      gamma: 0.65
  evaluate:
    data_loaders:
      test:
        split_key: test_idxs
        batch_size: 128
        num_workers: 4
        sampler:
          _target_: torch.utils.data.sampler.SubsetRandomSampler
gpu_list: 
- 0
- 1
seed: null
dump_path: ./outputs/
